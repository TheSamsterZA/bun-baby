{
  "name": "bun-baby",
  "module": "index.html",
  "scripts": {
    "dev": "bun index.html",
    "build": "rm -rf dist/ && bun build index.html --outdir=dist/ --minify && cp robots.txt dist/"
  },
  "type": "module"
}
